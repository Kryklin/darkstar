package main

import (
	"fmt"
	"os"
	"strings"
	"encoding/json"
	"x:/Projects/darkstar/d-kasp-512/go" // This won't work as an import if not in GOPATH
)

// actually I'll just write a small standalone test that expects the binary to be in current dir
func main() {
    // I already have d-kasp-512-go.exe
}
